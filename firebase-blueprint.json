{
  "entities": {
    "lead": {
      "title": "Lead",
      "description": "Information captured from potential clients via the contact form.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The full name of the lead."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The contact email address of the lead."
        },
        "company": {
          "type": "string",
          "description": "The company name of the lead (optional)."
        },
        "message": {
          "type": "string",
          "description": "A brief message or project description from the lead."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The server timestamp when the lead was created."
        }
      },
      "required": ["name", "email", "message", "createdAt"]
    }
  },
  "firestore": {
    "leads/{leadId}": {
      "schema": "lead",
      "description": "Collection of lead submissions from the landing page contact form."
    }
  }
}
